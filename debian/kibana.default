# Defaults for #PACKAGE# initscript
# sourced by /etc/init.d/#PACKAGE#
# installed at /etc/default/#PACKAGE# by the maintainer scripts

#
# This is a POSIX shell fragment
#
START="false"
PIDFILE="/var/log/kibana/kibana.pid"
KIBANA_FULLPATH="/usr/share/kibana/kibana.rb"
KIBANA_DIR=$(dirname $KIBANA_FULLPATH)
KIBANA_BINARY=$(basename $KIBANA_FULLPATH)
USER="kibana"
GROUP="kibana"

# Additional options that are passed to the Daemon.
DAEMON_ARGS="-C $KIBANA_DIR $KIBANA_BINARY"
